import { PokemonSpeciesCollection } from './pokemon-species-collection';

describe('PokemonSpeciesCollection', () => {
  it('should create an instance', () => {
    expect(new PokemonSpeciesCollection()).toBeTruthy();
  });
});
